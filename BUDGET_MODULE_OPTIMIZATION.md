# 预算模块深度优化报告

日期：2026-08-29

## 优化目标

本次修改覆盖预算模块的数据模型、周期计算、数据库查询、ViewModel 状态、页面刷新、日期选择、分类预算详情和后台周期重置。目标是统一周/月/年逻辑，修复日期范围错误、异步刷新闪烁和大量数据下的卡顿。

## 发现的问题与修复

### 1. 周预算存在两套定位规则

问题：周预算同时支持 `year + month` 查询和 `start_time` 查询，其中 `month` 在部分代码里实际表示周序号，在其他代码里表示自然月。跨月、跨年以及不同地区周编号规则下会定位到错误记录。

修复：

- 周预算统一使用规范化后的周起始时间 `start_time` 作为周期键。
- 删除 DAO、Repository 和 ViewModel 中按 `year + weekNumber` 查询周预算的旧接口。
- 日期选择器直接传递 `startTime/endTime`，不再传周序号后反算日期。
- 周期前后移动改为按 7 个日历日移动。

### 2. 周/月/年状态分多次发布

问题：切换周期时依次更新类型、开始时间、结束时间、年份和月份。Observer 会短暂收到“新类型 + 旧日期”等不完整组合，触发错误查询和 UI 闪烁。

修复：

- 新增不可变 `PeriodSelection`，一次保存类型、年份、月份、开始和结束时间。
- 总预算、分类预算和统计查询只监听这一份原子周期状态。
- 页面只监听完整周期对象刷新日期和周期列表。
- 移除用于自动加载统计的 `observeForever`，避免永久观察和重复请求。

### 3. 日期边界计算不稳定

问题：

- 自定义月起始日为 29、30、31 时，二月会被 `Calendar` 自动滚到三月。
- 周范围展示和数据库查询可能分别重新计算，得到不同结果。
- 使用固定 `86,400,000ms` 计算“第几天”，在夏令时切换地区会错位。
- 无预算记录时，正确的已选日期范围会被覆盖为“未设置预算周期”。

修复：

- 月起始日按当月最后一天夹取，不允许日期宽松滚月。
- 周期结束统一使用“下一周期开始时间减 1ms”计算。
- 新增按日历日计数方法，替代固定毫秒天数。
- 详情页直接使用预算记录持久化的规范范围，不再按年月二次反算。
- 无预算时保留当前选择的日期范围，只将金额显示为 0。

### 4. 异步统计存在旧请求覆盖新请求

问题：快速切换周期或收支类型时，旧统计任务可能比新任务更晚完成，随后覆盖新页面数据。分类列表构建也有同样问题。

修复：

- 统计请求和分类列表请求分别加入 generation 版本号。
- 回到主线程前校验周期、收支类型和请求版本，过期结果直接丢弃。
- 周期切换时保留旧列表，直到 Room 返回新列表，避免先清空再显示造成闪烁。
- 文本只在内容变化时更新，进度条和 drawable 只在值变化时更新。

### 5. 分类预算列表存在 N+1 查询

问题：每个分类预算都会单独查询一次该分类的账单。分类数量越多，数据库查询次数越多，容易卡顿。

修复：

- 在 `BillDao` 新增 `GROUP BY category_id` 聚合查询。
- 一次查询得到当前周期所有分类的金额，再在内存中映射到列表。
- 总收入和总支出改用 SQL `SUM`，不再加载整批 Bill 后用 Java 求和。
- 支出与收入分类统计都按当前 `transaction_type` 查询，修复收入预算分类金额始终为 0 的问题。
- 同时缓存收入和支出分类元数据，预算快照作为分类数据尚未加载时的回退展示。

### 6. 数据库缺少预算统计索引

问题：账单范围聚合和预算周期查询缺少覆盖当前过滤条件的组合索引，数据增长后扫描成本较高。

修复：

- 数据库版本从 28 升级到 29。
- 新增账单预算统计索引：`user_id, type, excludeBudget, billTime, category_id`。
- 新增预算周期索引：`owner_id, transaction_type, budget_type, start_time, target_type`。
- 保留年月兼容查询索引，并提供 `MIGRATION_28_29`，不需要清空现有数据。

### 7. 写入完成前就触发统计刷新

问题：ViewModel 和 Repository 重复排队后台任务，统计可能在预算写入提交前执行，短暂显示旧金额。

修复：

- 总预算保存收敛为 Repository 内一次原子“查询并新增/更新”。
- 总预算和分类预算的“查重 + 写入”均放入 Room 事务，避免两个磁盘线程并发插入重复记录。
- Room 提交完成后才回调刷新剩余分配额和统计。
- 删除分类预算也在删除或软删除提交后刷新。
- 云端预算批量合并放入单个 Room 事务，避免每写一条数据就刷新一次页面。

### 8. 列表全量刷新与无效滚动

问题：横向周期列表每次调用 `notifyDataSetChanged()`，并始终执行平滑滚动，容易造成抖动。

修复：

- `MonthAdapter` 改用 `DiffUtil` 差量更新。
- 只有选中项不在可见范围时才滚动，并使用无动画定位。
- 年列表以当前选中年份为中心生成，历史年份选择后仍能正确高亮。

### 9. 分类预算详情与主页面统计口径不同

问题：详情页重新按周序号和年月计算范围，可能与主页面传入范围不一致；每日趋势同样使用固定 24 小时。

修复：

- 详情页优先使用预算记录保存的起止时间，仅对无时间的旧记录做回退计算。
- 月趋势支持自定义月起始日形成的跨月范围。
- 日趋势使用本地日历日起点映射，兼容夏令时。
- 趋势切换增加请求版本校验。
- 详情页携带并使用预算的收入/支出类型。

### 10. 周期重置任务可能延迟一周

问题：代码定义周日为周起点，但重置任务只允许周一执行；如果 WorkManager 周一未准时运行，会一直等到下一周。

修复：

- 只要旧窗口已过期就重置到当前周，不依赖任务恰好在某个星期运行。
- 有重置数据时主动安排预算同步任务。

### 11. 收入分类预算仍可能写入支出数据

问题：分类选择弹窗固定监听支出分类；自动分配创建 `Budget` 时没有显式写入当前收支类型，收入目标可能被保存为支出预算。

修复：

- 分类选择器根据当前预算的 `transaction_type` 选择收入或支出分类数据源。
- 自动分配和统一新增入口显式写入当前收支类型，不再依赖实体默认值。

### 12. 收入模式通过交换统计字段复用支出 UI

问题：收入模式会交换 `totalIncome` 与 `totalExpense`，导致标有“收入”的卡片显示支出；年度收入目标还硬编码为 `200000`。

修复：

- 收入和支出统计字段始终保留真实业务含义，不再为适配 UI 交换数据。
- `incomeBudget` 与 `expenseBudget` 分别保存收入目标和支出预算。
- 主进度、年度概览、明细卡片和预算建议根据当前收支类型选择对应金额。
- 删除年度收入硬编码目标，未选择的一侧显示实际金额但不伪造目标进度。

## 主要修改文件

- `utils/BudgetPeriodHelper.java`：统一周期边界、月末夹取、日历日计数。
- `ui/viewmodel/budget/BudgetViewModel.java`：原子周期状态、请求防竞态、统一周查询和写后刷新。
- `ui/activity/BudgetActivity.java`：稳定刷新、批量分类统计、收入分类支持和日期展示修复。
- `ui/fragment/BudgetDateSelectorFragment.java`：直接选择精确时间范围。
- `ui/fragment/CategorySelectFragment.java`：按收入/支出预算切换分类数据源。
- `ui/fragment/AddBudgetFragment.java`：保存和沿用预算均使用精确范围。
- `data/dao/BudgetDao.java`、`data/dao/BillDao.java`：精确周期查询与 SQL 聚合。
- `data/repository/budget/BudgetRepository.java`：原子保存、批量云合并和聚合数据接口。
- `data/database/AppDatabase.java`：29 版索引迁移。
- `ui/viewmodel/budget/CategoryBudgetDetailViewModel.java`：详情范围和趋势统计统一。
- `ui/adapter/budget/MonthAdapter.java`：差量刷新。
- `work/BudgetResetWorker.java`：过期检测和同步调度。

## 测试与验证

已新增 `BudgetPeriodHelperTest`，覆盖：

- 跨年周范围。
- 月起始日为 31 日时的二月边界。
- 闰年 366 天。
- 夏令时切换月份的日历日计数。

已完成 Java 源码语法检查，未发现语法错误。

完整 Gradle 编译和单元测试未能执行：工程原 `JAVA_HOME` 指向不存在的 JRE；改用本机 JDK 后，隔离 Gradle 缓存缺少 Android Gradle Plugin。机器用户缓存中存在插件，但使用该缓存的权限请求未获批准。按需求未启动 App。

## 建议验收场景

1. 连续快速切换周、月、年以及收入、支出，确认金额不回跳、列表不白屏。
2. 检查 12 月末到 1 月初的跨年周，主页面、日期选择器和详情页范围应完全一致。
3. 将月起始日设置为 29、30、31，检查二月及闰年二月。
4. 在分类数量和账单数量较多的账户上切换周期，确认列表加载无明显卡顿。
5. 新增、编辑、删除总预算和分类预算，确认保存后金额一次更新到最终值。
6. 检查收入预算分类金额和收入预算详情，不应再读取支出账单。
