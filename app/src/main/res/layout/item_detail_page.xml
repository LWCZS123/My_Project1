<?xml version="1.0" encoding="utf-8"?>
<!--
  item_detail_page.xml
  每一个 ViewPager2 分页的根布局。
  包含三个互斥视图：
    1. rv_page_icons    — 正常图标网格（LOADED）
    2. progress_page    — 加载中动画（LOADING / IDLE）
    3. view_page_error  — 加载失败提示（ERROR）
  由 DetailPagerAdapter.PageViewHolder.bind(PageState) 控制显隐。
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F6FA">

    <!-- ① 图标网格 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_page_icons"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:padding="8dp"
        android:scrollbars="none"
        android:visibility="gone" />

    <!-- ② 加载中：居中环形进度条 -->
    <ProgressBar
        android:id="@+id/progress_page"
        style="@style/Widget.AppCompat.ProgressBar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="visible" />

    <!-- ③ 加载失败：居中文字提示 -->
    <LinearLayout
        android:id="@+id/view_page_error"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:alpha="0.4"
            android:src="@android:drawable/stat_notify_error" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="加载失败，请重试"
            android:textColor="#AAAAAA"
            android:textSize="13sp" />

    </LinearLayout>

</FrameLayout>